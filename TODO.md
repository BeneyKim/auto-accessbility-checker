# TODO 및 이슈 분석 리포트

---

## 1. 인지적 접근성 일관성 진단 (Cognitive Navigation Consistency Check)

크롤링 완료 후 동적으로 수집한 이동 히스토리(`transitionLogs`)를 기반으로 **"동일 라벨, 다른 목적지 (Same Label, Different Target)"** 접근성 위배 사항(WCAG 3.2.4)을 진단하기 위해 테스트 및 구현을 시도했던 내용과 향후 해결 방향을 기록합니다.

### 1) 테스트 진행 결과 및 문제점
* **단위 테스트 (`analyzer.test.ts`, `report.test.ts`)**:
  * 사전에 정의된 모의(Mock) `TransitionLog` 데이터를 활용한 정적 검증 및 보고서(HTML/Markdown) 생성 기능은 100% 정상 통과했습니다.
* **실제 웹 크롤링 테스트 (`냉동고` 제품 대상)**:
  * 실제 기기 제어 웹 크롤링에서는 일관성 위배 사항이 보고서에 감지되지 않는 **미탐(False Negative)** 현상이 발생했습니다.
  * **원인 (루프 가드 충돌)**:
    * 현재 크롤러는 무한 루프를 차단하기 위해 조상 경로(`menuPath`)에 이미 클릭하려는 라벨(`triggerName`)이 존재할 경우 실행을 건너뜁니다.
    * `"기타"` 화면(`menuPath: [... "식품 추가", "기타"]`)에서 우상단의 `+` 버튼(라벨: `"식품 추가"`)을 만났을 때, 부모 경로에 이미 `"식품 추가"`가 있으므로 클릭 대상에서 **강제 제외(Skip)**되었습니다.
    * 이에 따라 실제 화면 이동(`transition`)이 수행되지 않아 로그가 누적되지 않았고, 불일치를 판단할 데이터가 없어 리포트 누락이 발생했습니다.

### 2) 향후 개선 및 기술적 제안 방향
크롤러가 동일 라벨을 가진 다른 화면의 버튼을 차단하지 않고 정상적으로 스캔하여 위배를 잡아내기 위한 해결 방안은 다음과 같습니다.
* **방안 A: 루프 가드(Loop Guard) 완화 (추천)**:
  * `menuPath.includes(triggerName)`의 전수 검사를 완화하여 바로 직전 부모(Immediate Parent) 또는 상위 2단계 이내에서만 루프 여부를 체크합니다.
  * 예시:
    ```typescript
    const isDirectLoop = frame.menuPath.length > 0 && frame.menuPath.at(-1) === triggerName;
    return !context.attemptedCandidates.has(key) && !isDirectLoop;
    ```
* **방안 B: 후보군 이름 수집 시 페이지 고유 맥락(DOM Context) 결합**:
  * 후보 버튼을 식별할 때 단순 대체 텍스트 외에, 현재 페이지 타이틀 또는 부모 컨테이너 고유 속성을 포함하여 중복 체크용 고유 ID를 내부 매핑용으로 생성합니다.
  * 예시: `"식품 추가"`가 아닌 `"식품 추가 (기타 카테고리)"` 형태로 내부 정규화하여 가드 오작동을 차단합니다.

### 3) 의사결정 사항 (Decision)
* **결정**: 인지적 접근성 검출 기능은 복잡한 크롤러 이동 가드와의 연계 및 예외 처리가 요구되므로, **현재 단계에서는 해당 코드를 모두 원복하고 보류**합니다.
* **사유**: 핵심 크롤링 엔진의 안정성, 정확성, 무한 루프 예외 차단 등의 **기본기(Base Traversal Engine)를 먼저 견고하게 다진 후에** 점진적으로 고급 접근성 진단 로직을 추가하기로 합의하였습니다.

---

## 2. 에너지 모니터링 진입 후 초기 팝업 닫힘 시 화면 캡처 누락 이슈

### 1) 현상 분석 및 검증 결과
* **현상**: 에너지 모니터링 진입 시 최초 1회 노출되는 다이얼로그/안내 팝업(class에 `EKznS show modal` 포함)이 열려 있는 상태에서 최초 화면 스캔이 수행됩니다. 이후 자동 크롤러가 `"확인"` 버튼을 클릭하여 팝업을 닫지만, 팝업이 닫힌 상태의 **순수 에너지 모니터링 메인 화면**은 단독 스크린 결과로 캡처되거나 스캔되지 않습니다.
* **로그 분석 결과**:
  * **Entry 259**: `에너지 모니터링` 진입 시 `overlayCount: 1` 상태로 최초 감지. 화면 타이틀은 `"제품"`으로 획득되어 `screenshot_21.jpeg`에 팝업이 포함된 상태로 저장됨.
  * **Entry 264**: 팝업 내 `"확인"` 버튼 후보 수집.
  * **Entry 265~266**: `"확인"` 클릭 후 `overlay-count-decreased`로 전이(`overlayCount: 0`). 타이틀이 `"에너지 모니터링"`으로 전환됨.
  * **Entry 268~270**: 화면 스캔을 다시 거치지 않고, 새로 수집된 메인 화면 후보군 중 `"추가 정보"`를 즉시 클릭하여 다음 깊이(depth 2, `사용자 가이드`)로 이동.
  * **결과**: 팝업이 닫힌 순수 메인 화면의 베이스라인 스크린 정보와 스크린샷이 리포트에 독립적으로 생성되지 못하는 현상 발생.

### 2) 기술적 원인 (Technical Root Cause)
* 현재 크롤러는 깊이(depth) 기준으로 상태를 관리합니다. `에너지 모니터링` 진입 시 이미 depth 1에서 스캔을 완료했기 때문에, 팝업 닫힘(`overlay-count-decreased`)으로 인해 화면의 실질적인 레이아웃이 변경되었음에도 동일 depth 내에서 추가적인 스크린 업데이트나 재캡처를 수행하지 않고 바로 후보 클릭을 진행합니다.

### 3) 개선 및 해결 방안 제안
* **방안 A: Overlay 감소(`overlay-count-decreased`) 발생 시 스크린샷 및 DOM 정보 업데이트**:
  * 크롤러 루프에서 overlay close 트리거 성공 후, 동일 depth에 머무를 경우 현재 스크린 노드의 스크린샷 및 DOM 체크 결과를 갱신(Rescan 및 Screenshot Overwrite)하는 로직을 추가합니다.
* **방안 B: 초기 자동 닫힘 팝업 전용 처리 핸들러 도입**:
  * 기기 제어 진입 시 나타나는 최초 안내 팝업 등은 크롤러의 공식 깊이 탐색에 포함시키기 전에, 탐색 시작 직후 동적으로 팝업 존재 여부를 체크하여 선제적으로 `"확인"` 또는 `"닫기"`를 수행한 뒤 본격적인 depth 0/1 스캔을 시작하도록 동기화 단계를 추가합니다.
