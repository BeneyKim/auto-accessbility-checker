# ThinQ Web IBM Accessibility Checker Version History

## v0.99.13 (2026-06-22)
- **문서화 및 행동 규칙/실수 리포트 정립**
  - 에이전트 행동 규칙 및 스타일 가이드인 `AGENTS.md`에 접근성 교차 진입 방지, 탭 격리 캐시, 스마트진단 8레벨 차단, Shadow DOM 중복 제거 등 12가지 프로젝트 최적화 규칙 완벽 명시.
  - 에이전트 개발 실수를 정밀 분석하고 교훈 및 Lessons Learned를 기록한 `AGENT_MISTAKES.md`를 신규 구축 및 연계.
  - 특히 "로그 미확인 추측 기반 수정 금지(선 로그 대조 후 분석)" 4대 절대 수칙(로그 분석, 로그 동작 검증, 실제 로그 DOM 모킹, 사이드 이펙트 verify 교차 검증)을 1순위 행동 원칙으로 확정하여 품질 안정성을 영구 확보.
  - `TODO.md`를 향후 추가 개선 과제(백로그)만 담도록 정비하고 요약 테이블을 추가하여 시각화.

## v0.99.12 (2026-06-21)
- **워시타워 오진입 및 무한 루프 차단 (이슈 1, 2, 3, 4)**
  - 비활성 탭 버튼(`aria-selected="false"`) 하위의 모든 요소를 스킵하도록 필터링을 강화하여, 시작 단계에서 엉뚱한 탭 메뉴로 진입하는 오작동 차단.
  - 단순 URL 기반 목록 판별을 폐지하고, 검색창/목록 요소의 DOM 구조를 판단하는 `isDynamicListOrSearchPage`를 도입하여 세탁/건조 탭 간 무한 루프 방지.
  - 스마트진단 오진입 방지를 위해 부모 조상 탐색 깊이를 8단계로 확장하고 sds, smartdiagnosis class/id를 검출하여 하드웨어 제어 기능 실행 차단.
  - 캐시 키(`semanticLayoutKey`)에 현재 활성화된 탭 명칭(`(tab:selectedTab)`)을 바인딩하여 세탁/건조 이력 화면 간 세맨틱 캐시 충돌 및 진입 누락 방지.
- **냉장고 식품 카드 중복 클릭 개선 (이슈 5)**
  - Shadow DOM 호환 `shadowContains` 헬퍼 및 텍스트 태그 필터링을 통해 조상 6레벨 이내 중복 이름 엘리먼트들을 효과적으로 중복 제거하여 냉장고 식품 카드 중복 클릭 해결.
- **스크린샷 캡쳐 안정화 및 리포트 저장 (이슈 6)**
  - MV3 환경에서 탭 전환/리로딩 도중 스크린샷 캡쳐 권한이 소실되지 않도록 `public/manifest.json`에 `<all_urls>` 권한 적용.

## v0.99.10 (2026-06-01)
- **에너지 모니터링 초기 팝업 캡쳐 누락 해결**
  - 에너지 모니터링 진입 시 노출되는 팝업이 닫힐 때(`overlay-count-decreased`) 동일 depth에서 스크린 노드를 다시 스캔하고 스크린샷을 업데이트하도록 개선하여 팝업이 열린 상태와 닫힌 상태를 모두 캡쳐하도록 수정.
- **크롤러 UX 연동 및 성능 최적화**
  - Chrome Side Panel API를 연동하여 크롤러 진행 로그를 사이드바에서 모니터링할 수 있도록 고도화.
  - 식기세척기 코스설정 등 커스텀 콤마 기호 엘리먼트가 스위치 스킵 필터에 걸려 지연되는 버그(`isStrongInteractive` 우회) 및 공기청정기 취침예약 중복 진입 개선.

## v0.99.6 (2026-05-31)
- **식품 관리 중복 스캔 제거 및 크롤러 리포트 고도화**
  - **오버레이 복귀 시 부모 화면 중복 스캔 제거**: 오버레이/팝업창(수량, 보관 시작일 등)을 닫고 부모 화면으로 돌아올 때 DOM 변화로 인해 시그니처가 바뀌더라도 타이틀이 부모 화면과 같으면 중복 스캔을 생략하도록 최적화.
  - **라벨 정규화 적용**: 동적 값들을 포함하는 콤마 `,` 구분형 텍스트 및 날짜 접미사를 제거하여 리포트 경로(`menuPath`) 및 화면 식별자를 간결하게 단일화.
  - **캘린더/일자 선택 보존**: 날짜 피커 버튼 등 전체가 날짜로만 구성된 요소들이 빈 텍스트로 오작동하는 현상을 막기 위한 안전장치 추가.

## v0.99.2 (2026-05-25)
- **리포트 패키징 및 스크린샷 경로 최적화 패치**
  - **ZIP 보고서 패키징 및 스크린샷 상대 경로 분리**: JSON, MD, HTML 보고서에서 거대한 base64 스크린샷 데이터를 분리하여 ZIP 파일 내 `screenshots/screenshot_x.png` 상대 경로로 저장하고, 보고서들은 이 상대 경로를 가리키도록 구조 개선. 이로 인해 3.5MB가 넘던 MD 보고서 파일 등이 수십 KB 수준으로 줄어들어 파싱 오류 및 에디터 프리징 현상을 완전히 해결.
  - **반복 리스트 샘플링 레이스 컨디션 해결**: 무작위 선택 시 동일한 후보군이 안정적으로 클릭되도록 deterministic 해시 알고리즘과 페이지 전역 시드(`window.__thinqSeed__`)를 결합.
  - **식품 관리 등 중복 진입 필터 고도화**: 단순 명칭이 동일하고 부모/조부모 레벨이 같은 후보군에 대해 첫 번째 후보군만 대표 탐색하도록 해 중복 진입 방지.
  - **UI 개선 및 정리**: 팝업 UI에서 사용하지 않는 Recon Scan 버튼을 숨김 처리하고, Report 다운로드 버튼을 프리미엄 그린 그라데이션 스타일로 세련되게 변경.

## v0.99.1 (2026-05-25)
- **크롤러 지연 및 리포트 파싱 오류 개선 패치 (Phase 12-13)**
- **취소/저장 버튼 우선순위 공통 정책 적용**: 
  - 한 화면 내에 취소 성격의 버튼과 저장 성격의 버튼이 공존하는 경우, 가전 상태 변경(식품 추가 등)에 따른 탐색 무한 루프를 방지하기 위해 저장 버튼을 클릭 대상에서 제외하고 취소 경로로 퇴출 처리.
- **반복 리스트 무작위 샘플링 최적화**:
  - 5개 이상의 동일 패턴 반복 리스트(예: 냉장고 식품 추가의 77개 야채 목록) 탐색 시 첫 번째, 마지막, 그리고 중간의 **1개 무작위(Random) 항목**만 진입하도록 필터링을 도입하여 크롤링 시간을 획기적으로 줄이고 다변화된 테스트 커버리지를 확보.
- **보고서 파일 크기 최적화**:
  - MD 보고서 파일 내 Raw JSON 검사 결과 덤프를 생략하고 중요 오류 요약 표(Table)로 대체하여 파일 크기를 30MB에서 1MB 미만으로 경량화함으로써 텍스트 에디터 프리징 현상 해결.
- **Chrome Storage 할당량 확장**:
  - `unlimitedStorage` 권한을 Manifest에 추가하여 대용량 크롤링 시 스토리지 할당량 초과(Quota Exceeded) 버그 해결.

## v0.99 (2026-05-24)
- **Zero-Base Traversal Engine Redesign (Phase 1-6) 완료**
- **Recon Mode (사전 탐색 도구)**:
  - 페이지를 직접 클릭해 들어가지 않고 현재 화면의 모든 인터랙티브 및 비인터랙티브 요소를 진단하고 추출할 수 있는 Recon Scan 기능 추가.
- **금지 버튼 레지스트리 (Forbidden Registry)**:
  - 22개의 비정상 경로/외부 서비스 이탈 금지 규칙을 관리할 수 있는 전용 레지스트리 모듈 도입.
  - 소모품 정보, 가전세척 신청, 스마트 진단, ThinQ PLAY, 스마트 루틴, 이전 날/달 등의 비정상 루프 유발 제어 장치 추가.
- **Playwright E2E 자동화 파이프라인**:
  - 구글 세션 로그인 저장 스크립트 및 자동으로 페이지를 로드하여 Recon 데이터 스캔을 수행하는 Playwright spec 추가.
- **다중 복구 및 셀프 힐링 (Self-Healing Restore)**:
  - 뒤로가기 버튼 정규식 완화 및 `document.body` 전역 검출로 ThinQ SPA 쉘 외부 뒤로가기 대응.
  - 복합 복구 로직(`overlay-close` -> `Escape` -> `back-button` -> `history-back` -> `tab-reentry`) 구축.
  - 동일한 타이틀을 가진 미지원 기능 알럿 경고창 등의 복구 예외를 판정하기 위해 `verifyRestore` 내에 오버레이 개수 비교 가드 추가.
  - 알럿 팝업 닫기용 `"확인/OK"` 폴백 클릭 기능 구현.
- **성능 최적화**:
  - 클릭 무반응 요소 및 이미 활성화된 탭 클릭 시 대기 시간을 6초에서 1.8초로 단축 (`NO_CHANGE_STABLE_MS`).
  - 비인터랙티브 요소(일반 div 등)에 대한 키보드 retry 생략으로 탐색 지연 최소화.
- **A11y 대시보드 리포터**:
  - 기존 33MB 분량의 raw JSON 출력 테이블을 획기적으로 개선하여, PASS 결과를 생략한 2~3MB 수준의 가볍고 빠른 반응형 대시보드 구현.
  - 좌측 스크린 리스트 사이드바(이슈 개수별 배지 표기) 및 실시간 검색 필터 제공.
  - 스크린샷 뷰포트 정합 배치 및 클릭 시 전체 화면 확대 모달(Lightbox) 기능 추가.
